#include <stdio.h>
int rabbit(n,f){
    int r=(f-(n*2))/2;
    return r;
}

int main(int argc, char const *argv[])
{
    int n1,f1,n2,f2,r1,r2;
    scanf("%d%d%d%d",&n1,&f1,&n2,&f2);
    r1=rabbit(n1,f1);
    r2=rabbit(n2,f2);
    printf("%d%d%d%d",n1-r1,r1,n1-r2,r2);
    return 0;
}
